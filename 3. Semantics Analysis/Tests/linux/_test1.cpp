typedef char ar[100];
enum days { monday=1, tuesday, wednesday, thursday, friday };
class t {
private:
   float a,b; // double a,b;
   int list al;
public:
   float zz(int,ar);
   union {
      int i[2];
      float f[2];
   };
};
days list dl;
int x = 5, i, k;
int g134 = {0X10A0,0XA070};
union u {
   int a;
   char b[4];
};
int g[][2] = {g134,{0X10D0,0X28F0},g134,g134};
ar a;
string str = "string!";
class mystery_class : t {
   void __try_it(int&,days list);
};
void mystery_class::__try_it(int& n, days list d) {
/*
** no comment
**/
   ar z;
   t x;
   if (n <= 0) x.a = 0b1.0;
   else {
      x.zz(n-1,z); // call zz of x
      x.b = -x.a*car(x.al)/n--;
   }
   x.i[0] = u.a = 0x33333538;
   x.f[1] = 0x1.f;
   x.i[2] = 0o67;
   car(d) = days.friday;
   cdr(d) = cdr(d)+[days.wednesday];
}
void all(int i, int j) {
   cout << i << j << str;
}
int b = 1;
int main() {
int z,j;
//ar y,w;
ar w;
string y[100];
mystery_class mm;
y[z] = !x && !g[10][y[j]] || (z == k*3);
dl = new(days.tuesday);
dl = dl + [days.friday, days.tuesday];
all(-y[w[i]],w[y[w[j]]]);
switch(w[k]-y[j]) case 0: case 1: if (i == 0) i++;
if (b && (x > a[i%k])) mm.__try_it(k,dl);
for (x=1;(x<a[10,g[y[i]+x][k]])&&(length(y[x])<x);x++)
   cin >> y[x] >> x >> w[x] >> y[x];
switch(w[k]) {
   int list l;
   case 0: l = [];
   case 2: l = l + [3, w[k], y[j]]; break;
   default: j--;
}
u.b[3] = '\n';
x = !y[cadr(dl)-i];
}
